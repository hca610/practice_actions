name: Do test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: lint
        run: |
          echo "lint"
      - name: test
        run: |
          echo "test"
      - name: build
        run: |
          echo "build"